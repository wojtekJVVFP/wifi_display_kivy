<WifiServer>:
    button: button1 #name from py: id from kv file, the name can be the same as id
    label: label1
    image: image

    Image:
        id: image
        x: 0
        y: 0
        source: 'obrazy/linie.jpg'
        size: (root.size[0]-100, root.size[1]-100)
    Button:
        id: button1
        text: 'button'
        x: image.width+10
        y: 0
    Label:
        id: label1
        text: 'jakies pole'
        x: button1.x
        y: button1.y + button1.height



